[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
# Ensure project root is on path for imports
# (If needed, we can add a conftest.py to tweak sys.path further.)
